<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="u">
	<select id="user_list" resultType="users">
		select * from users
	</select>
	<insert id="user_insert" parameterType="users">
		INSERT INTO Users (
		USER_ID,
		USER_NAME,
		ID,
		USER_PWD,
		USER_EMAIL,
		BIRTH_DATE,
		JOIN_DATE
		) VALUES
		(
		users_seq.NEXTVAL,
		#{username},
		#{id},
		#{userpwd},
		#{useremail},
		TO_DATE(#{birthdate}, 'YYYY-MM-DD'),
		SYSDATE
		)
	</insert>



	<select id="Duplicate_check" parameterType="String"
		resultType="int">
		SELECT COUNT(*) FROM Users WHERE ID = #{id}
	</select>

	
	<select id="select_Username" parameterType="String" resultType="users">
    SELECT *
    FROM Users
    WHERE ID = #{username}
</select>
	



</mapper>























